# auth

Authentication — email/password, Google, Apple sign-in, phone OTP. Manages user sessions and role-based access (parent vs child).

## Structure

```
auth/
  hooks/        # React hooks (data fetching, mutations, local state)
  components/   # UI components specific to this feature
  types.ts      # TypeScript type definitions
  index.ts      # Barrel export
```

## Status

Scaffolded — no business logic implemented yet.
